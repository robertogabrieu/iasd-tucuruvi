services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env.local
    depends_on:
      - mailpit

  mailpit:
    image: axllent/mailpit:latest
    ports:
      - "8025:8025"
      - "1025:1025"
    restart: unless-stopped
